/* tslint:disable */
/* eslint-disable */
/**
 * KeepChill API
 * The KeepChill API lets you upload images, apply watermarks, and retrieve the processed results. Image bytes never traverse KeepChill servers — clients upload directly to Google Cloud Storage via short-lived signed URLs.  ## Flow  1. Exchange your API key for a short-lived JWT at `POST /v1/auth/token`. 2. Request a batch of signed upload URLs at `POST /v1/watermarks/signed-urls`. 3. `PUT` each file directly to its signed URL (Google Cloud Storage). 4. Poll `GET /v1/jobs/{job_id}` (or your webhook) until the job completes. 5. Download processed files via the URLs returned on the job/file resources.  ## Authentication  - `ApiKeyAuth` (header `x-striker-api-key`) — used **only** at `/v1/auth/token`.   Never send the API key to any other endpoint. - `BearerAuth` (JWT in `Authorization: Bearer <token>`) — used for every   other endpoint. Tokens are RS256, currently valid for 1 hour.  ## Versioning  All endpoints are prefixed with `/v1`. Breaking changes ship as `/v2`. The OpenAPI document itself is versioned independently in `info.version`. 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@keepchill.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { SignedUpload } from './SignedUpload';
import {
    SignedUploadFromJSON,
    SignedUploadFromJSONTyped,
    SignedUploadToJSON,
    SignedUploadToJSONTyped,
} from './SignedUpload';
import type { SignedUploadError } from './SignedUploadError';
import {
    SignedUploadErrorFromJSON,
    SignedUploadErrorFromJSONTyped,
    SignedUploadErrorToJSON,
    SignedUploadErrorToJSONTyped,
} from './SignedUploadError';
import type { SkippedFile } from './SkippedFile';
import {
    SkippedFileFromJSON,
    SkippedFileFromJSONTyped,
    SkippedFileToJSON,
    SkippedFileToJSONTyped,
} from './SkippedFile';

/**
 * 
 * @export
 * @interface SignedUrlsResponse
 */
export interface SignedUrlsResponse {
    /**
     * 
     * @type {string}
     * @memberof SignedUrlsResponse
     */
    job_id: string;
    /**
     * 
     * @type {Array<SignedUpload>}
     * @memberof SignedUrlsResponse
     */
    uploads: Array<SignedUpload>;
    /**
     * Present when one or more files failed to produce a signed URL.
     * @type {Array<SignedUploadError>}
     * @memberof SignedUrlsResponse
     */
    errors?: Array<SignedUploadError>;
    /**
     * Files that were not reserved due to quota cap.
     * @type {Array<SkippedFile>}
     * @memberof SignedUrlsResponse
     */
    skipped?: Array<SkippedFile>;
}

/**
 * Check if a given object implements the SignedUrlsResponse interface.
 */
export function instanceOfSignedUrlsResponse(value: object): value is SignedUrlsResponse {
    if (!('job_id' in value) || value['job_id'] === undefined) return false;
    if (!('uploads' in value) || value['uploads'] === undefined) return false;
    return true;
}

export function SignedUrlsResponseFromJSON(json: any): SignedUrlsResponse {
    return SignedUrlsResponseFromJSONTyped(json, false);
}

export function SignedUrlsResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): SignedUrlsResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'job_id': json['job_id'],
        'uploads': ((json['uploads'] as Array<any>).map(SignedUploadFromJSON)),
        'errors': json['errors'] == null ? undefined : ((json['errors'] as Array<any>).map(SignedUploadErrorFromJSON)),
        'skipped': json['skipped'] == null ? undefined : ((json['skipped'] as Array<any>).map(SkippedFileFromJSON)),
    };
}

export function SignedUrlsResponseToJSON(json: any): SignedUrlsResponse {
    return SignedUrlsResponseToJSONTyped(json, false);
}

export function SignedUrlsResponseToJSONTyped(value?: SignedUrlsResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'job_id': value['job_id'],
        'uploads': ((value['uploads'] as Array<any>).map(SignedUploadToJSON)),
        'errors': value['errors'] == null ? undefined : ((value['errors'] as Array<any>).map(SignedUploadErrorToJSON)),
        'skipped': value['skipped'] == null ? undefined : ((value['skipped'] as Array<any>).map(SkippedFileToJSON)),
    };
}

