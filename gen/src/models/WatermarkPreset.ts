/* tslint:disable */
/* eslint-disable */
/**
 * KeepChill API
 * The KeepChill API lets you upload images, apply watermarks, and retrieve the processed results. Image bytes never traverse KeepChill servers — clients upload directly to Google Cloud Storage via short-lived signed URLs.  ## Flow  1. Exchange your API key for a short-lived JWT at `POST /v1/auth/token`. 2. Request a batch of signed upload URLs at `POST /v1/watermarks/signed-urls`. 3. `PUT` each file directly to its signed URL (Google Cloud Storage). 4. Poll `GET /v1/jobs/{job_id}` (or your webhook) until the job completes. 5. Download processed files via the URLs returned on the job/file resources.  ## Authentication  - `ApiKeyAuth` (header `x-striker-api-key`) — used **only** at `/v1/auth/token`.   Never send the API key to any other endpoint. - `BearerAuth` (JWT in `Authorization: Bearer <token>`) — used for every   other endpoint. Tokens are RS256, currently valid for 1 hour.  ## Versioning  All endpoints are prefixed with `/v1`. Breaking changes ship as `/v2`. The OpenAPI document itself is versioned independently in `info.version`. 
 *
 * The version of the OpenAPI document: 1.1.0
 * Contact: support@keepchill.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


/**
 * Watermark protection profile, supplied via `config.preset`. Business-intent
 * presets (recommended) are self-describing and select the asset type and
 * protection profile. Legacy technical presets remain supported. An
 * unrecognised value is ignored by the worker (the field is advisory).
 * 
 * @export
 */
export const WatermarkPreset = {
    photographer_marketplace: 'photographer_marketplace',
    creator_marketplace: 'creator_marketplace',
    photographer_protection: 'photographer_protection',
    creator_protection: 'creator_protection',
    marketplace_friendly: 'marketplace_friendly',
    sports_marketplace_default: 'sports_marketplace_default',
    balanced_protection: 'balanced_protection',
    high_risk_event: 'high_risk_event'
} as const;
export type WatermarkPreset = typeof WatermarkPreset[keyof typeof WatermarkPreset];


export function instanceOfWatermarkPreset(value: any): boolean {
    for (const key in WatermarkPreset) {
        if (Object.prototype.hasOwnProperty.call(WatermarkPreset, key)) {
            if (WatermarkPreset[key as keyof typeof WatermarkPreset] === value) {
                return true;
            }
        }
    }
    return false;
}

export function WatermarkPresetFromJSON(json: any): WatermarkPreset {
    return WatermarkPresetFromJSONTyped(json, false);
}

export function WatermarkPresetFromJSONTyped(json: any, ignoreDiscriminator: boolean): WatermarkPreset {
    return json as WatermarkPreset;
}

export function WatermarkPresetToJSON(value?: WatermarkPreset | null): any {
    return value as any;
}

export function WatermarkPresetToJSONTyped(value: any, ignoreDiscriminator: boolean): WatermarkPreset {
    return value as WatermarkPreset;
}

