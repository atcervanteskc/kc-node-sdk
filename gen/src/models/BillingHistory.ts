/* tslint:disable */
/* eslint-disable */
/**
 * KeepChill API
 * The KeepChill API lets you upload images, apply watermarks, and retrieve the processed results. Image bytes never traverse KeepChill servers — clients upload directly to Google Cloud Storage via short-lived signed URLs.  ## Flow  1. Exchange your API key for a short-lived JWT at `POST /v1/auth/token`. 2. Request a batch of signed upload URLs at `POST /v1/watermarks/signed-urls`. 3. `PUT` each file directly to its signed URL (Google Cloud Storage). 4. Poll `GET /v1/jobs/{job_id}` (or your webhook) until the job completes. 5. Download processed files via the URLs returned on the job/file resources.  ## Authentication  - `ApiKeyAuth` (header `x-striker-api-key`) — used **only** at `/v1/auth/token`.   Never send the API key to any other endpoint. - `BearerAuth` (JWT in `Authorization: Bearer <token>`) — used for every   other endpoint. Tokens are RS256, currently valid for 1 hour.  ## Versioning  All endpoints are prefixed with `/v1`. Breaking changes ship as `/v2`. The OpenAPI document itself is versioned independently in `info.version`. 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@keepchill.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { BillingHistoryEntry } from './BillingHistoryEntry';
import {
    BillingHistoryEntryFromJSON,
    BillingHistoryEntryFromJSONTyped,
    BillingHistoryEntryToJSON,
    BillingHistoryEntryToJSONTyped,
} from './BillingHistoryEntry';

/**
 * 
 * @export
 * @interface BillingHistory
 */
export interface BillingHistory {
    /**
     * 
     * @type {Array<BillingHistoryEntry>}
     * @memberof BillingHistory
     */
    entries: Array<BillingHistoryEntry>;
    /**
     * Cursor for the next page. Absent on the last page.
     * @type {string}
     * @memberof BillingHistory
     */
    next_cursor?: string;
}

/**
 * Check if a given object implements the BillingHistory interface.
 */
export function instanceOfBillingHistory(value: object): value is BillingHistory {
    if (!('entries' in value) || value['entries'] === undefined) return false;
    return true;
}

export function BillingHistoryFromJSON(json: any): BillingHistory {
    return BillingHistoryFromJSONTyped(json, false);
}

export function BillingHistoryFromJSONTyped(json: any, ignoreDiscriminator: boolean): BillingHistory {
    if (json == null) {
        return json;
    }
    return {
        
        'entries': ((json['entries'] as Array<any>).map(BillingHistoryEntryFromJSON)),
        'next_cursor': json['next_cursor'] == null ? undefined : json['next_cursor'],
    };
}

export function BillingHistoryToJSON(json: any): BillingHistory {
    return BillingHistoryToJSONTyped(json, false);
}

export function BillingHistoryToJSONTyped(value?: BillingHistory | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'entries': ((value['entries'] as Array<any>).map(BillingHistoryEntryToJSON)),
        'next_cursor': value['next_cursor'],
    };
}

