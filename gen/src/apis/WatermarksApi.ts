/* tslint:disable */
/* eslint-disable */
/**
 * KeepChill API
 * The KeepChill API lets you upload images, apply watermarks, and retrieve the processed results. Image bytes never traverse KeepChill servers — clients upload directly to Google Cloud Storage via short-lived signed URLs.  ## Flow  1. Exchange your API key for a short-lived JWT at `POST /v1/auth/token`. 2. Request a batch of signed upload URLs at `POST /v1/watermarks/signed-urls`. 3. `PUT` each file directly to its signed URL (Google Cloud Storage). 4. Poll `GET /v1/jobs/{job_id}` (or your webhook) until the job completes. 5. Download processed files via the URLs returned on the job/file resources.  ## Authentication  - `ApiKeyAuth` (header `x-striker-api-key`) — used **only** at `/v1/auth/token`.   Never send the API key to any other endpoint. - `BearerAuth` (JWT in `Authorization: Bearer <token>`) — used for every   other endpoint. Tokens are RS256, currently valid for 1 hour.  ## Versioning  All endpoints are prefixed with `/v1`. Breaking changes ship as `/v2`. The OpenAPI document itself is versioned independently in `info.version`. 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@keepchill.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  SignedUrlsRequest,
  SignedUrlsResponse,
} from '../models/index';
import {
    SignedUrlsRequestFromJSON,
    SignedUrlsRequestToJSON,
    SignedUrlsResponseFromJSON,
    SignedUrlsResponseToJSON,
} from '../models/index';

export interface CreateSignedUploadUrlsRequest {
    signedUrlsRequest: SignedUrlsRequest;
}

/**
 * WatermarksApi - interface
 * 
 * @export
 * @interface WatermarksApiInterface
 */
export interface WatermarksApiInterface {
    /**
     * Reserves quota/credits, creates a job, and returns one signed `PUT` URL per file. Upload each file directly to its URL with the `Content-Type` you declared. URLs expire after 1 hour.  - Max 10 files per request. - Accepted content types: `image/jpeg`, `image/png`, `image/webp`. - Max 20 MB per file.  If your quota cannot cover the full batch, the response is `207` with a `skipped` array enumerating the files that were not reserved. 
     * @summary Create signed upload URLs for a batch of images
     * @param {SignedUrlsRequest} signedUrlsRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WatermarksApiInterface
     */
    createSignedUploadUrlsRaw(requestParameters: CreateSignedUploadUrlsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SignedUrlsResponse>>;

    /**
     * Reserves quota/credits, creates a job, and returns one signed `PUT` URL per file. Upload each file directly to its URL with the `Content-Type` you declared. URLs expire after 1 hour.  - Max 10 files per request. - Accepted content types: `image/jpeg`, `image/png`, `image/webp`. - Max 20 MB per file.  If your quota cannot cover the full batch, the response is `207` with a `skipped` array enumerating the files that were not reserved. 
     * Create signed upload URLs for a batch of images
     */
    createSignedUploadUrls(requestParameters: CreateSignedUploadUrlsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SignedUrlsResponse>;

}

/**
 * 
 */
export class WatermarksApi extends runtime.BaseAPI implements WatermarksApiInterface {

    /**
     * Reserves quota/credits, creates a job, and returns one signed `PUT` URL per file. Upload each file directly to its URL with the `Content-Type` you declared. URLs expire after 1 hour.  - Max 10 files per request. - Accepted content types: `image/jpeg`, `image/png`, `image/webp`. - Max 20 MB per file.  If your quota cannot cover the full batch, the response is `207` with a `skipped` array enumerating the files that were not reserved. 
     * Create signed upload URLs for a batch of images
     */
    async createSignedUploadUrlsRaw(requestParameters: CreateSignedUploadUrlsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SignedUrlsResponse>> {
        if (requestParameters['signedUrlsRequest'] == null) {
            throw new runtime.RequiredError(
                'signedUrlsRequest',
                'Required parameter "signedUrlsRequest" was null or undefined when calling createSignedUploadUrls().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/watermarks/signed-urls`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SignedUrlsRequestToJSON(requestParameters['signedUrlsRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SignedUrlsResponseFromJSON(jsonValue));
    }

    /**
     * Reserves quota/credits, creates a job, and returns one signed `PUT` URL per file. Upload each file directly to its URL with the `Content-Type` you declared. URLs expire after 1 hour.  - Max 10 files per request. - Accepted content types: `image/jpeg`, `image/png`, `image/webp`. - Max 20 MB per file.  If your quota cannot cover the full batch, the response is `207` with a `skipped` array enumerating the files that were not reserved. 
     * Create signed upload URLs for a batch of images
     */
    async createSignedUploadUrls(requestParameters: CreateSignedUploadUrlsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SignedUrlsResponse> {
        const response = await this.createSignedUploadUrlsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
