/* tslint:disable */
/* eslint-disable */
/**
 * KeepChill API
 * The KeepChill API lets you upload images, apply watermarks, and retrieve the processed results. Image bytes never traverse KeepChill servers — clients upload directly to Google Cloud Storage via short-lived signed URLs.  ## Flow  1. Exchange your API key for a short-lived JWT at `POST /v1/auth/token`. 2. Request a batch of signed upload URLs at `POST /v1/watermarks/signed-urls`. 3. `PUT` each file directly to its signed URL (Google Cloud Storage). 4. Poll `GET /v1/jobs/{job_id}` (or your webhook) until the job completes. 5. Download processed files via the URLs returned on the job/file resources.  ## Authentication  - `ApiKeyAuth` (header `x-striker-api-key`) — used **only** at `/v1/auth/token`.   Never send the API key to any other endpoint. - `BearerAuth` (JWT in `Authorization: Bearer <token>`) — used for every   other endpoint. Tokens are RS256, currently valid for 1 hour.  ## Versioning  All endpoints are prefixed with `/v1`. Breaking changes ship as `/v2`. The OpenAPI document itself is versioned independently in `info.version`. 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@keepchill.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AccessTokenResponse,
} from '../models/index';
import {
    AccessTokenResponseFromJSON,
    AccessTokenResponseToJSON,
} from '../models/index';

/**
 * AuthenticationApi - interface
 * 
 * @export
 * @interface AuthenticationApiInterface
 */
export interface AuthenticationApiInterface {
    /**
     * Exchanges a long-lived API key (sent in the `x-striker-api-key` header) for an RS256 JWT valid for 1 hour. Use the returned `access_token` as a `Bearer` credential on all other endpoints. 
     * @summary Exchange an API key for a short-lived access token
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    createAccessTokenRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccessTokenResponse>>;

    /**
     * Exchanges a long-lived API key (sent in the `x-striker-api-key` header) for an RS256 JWT valid for 1 hour. Use the returned `access_token` as a `Bearer` credential on all other endpoints. 
     * Exchange an API key for a short-lived access token
     */
    createAccessToken(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccessTokenResponse>;

}

/**
 * 
 */
export class AuthenticationApi extends runtime.BaseAPI implements AuthenticationApiInterface {

    /**
     * Exchanges a long-lived API key (sent in the `x-striker-api-key` header) for an RS256 JWT valid for 1 hour. Use the returned `access_token` as a `Bearer` credential on all other endpoints. 
     * Exchange an API key for a short-lived access token
     */
    async createAccessTokenRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccessTokenResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-striker-api-key"] = await this.configuration.apiKey("x-striker-api-key"); // ApiKeyAuth authentication
        }

        const response = await this.request({
            path: `/v1/auth/token`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccessTokenResponseFromJSON(jsonValue));
    }

    /**
     * Exchanges a long-lived API key (sent in the `x-striker-api-key` header) for an RS256 JWT valid for 1 hour. Use the returned `access_token` as a `Bearer` credential on all other endpoints. 
     * Exchange an API key for a short-lived access token
     */
    async createAccessToken(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccessTokenResponse> {
        const response = await this.createAccessTokenRaw(initOverrides);
        return await response.value();
    }

}
